# Mathematics Sample

## Algebra

The quadratic formula is

$$
x=\frac{-b\pm\sqrt{b^2-4ac}}{2a}
$$

If

$$
a^2+b^2=c^2
$$

then the triangle is right-angled.

---

## Calculus

The derivative of a function is

$$
\frac{d}{dx}(x^3)=3x^2
$$

The definite integral is

$$
\int_0^1 x^2\,dx=\frac13
$$

The limit

$$
\lim_{x\to0}\frac{\sin x}{x}=1
$$

---

## Linear Algebra

A matrix

$$
A=
\begin{bmatrix}
1 & 2\\
3 & 4
\end{bmatrix}
$$

Its determinant is

$$
\det(A)=1\cdot4-2\cdot3=-2
$$

---

## Series

The arithmetic series is

$$
\sum_{i=1}^{n} i=\frac{n(n+1)}{2}
$$

The geometric series is

$$
\sum_{k=0}^{\infty}x^k=\frac1{1-x}
\qquad(|x|<1)
$$

---

## Probability

The probability mass function is

$$
P(X=x)=\binom{n}{x}p^x(1-p)^{n-x}
$$

The expected value is

$$
E[X]=\sum_i x_iP(X=x_i)
$$

---

## Statistics

The mean is

$$
\mu=\frac1n\sum_{i=1}^{n}x_i
$$

The standard deviation is

$$
\sigma=\sqrt{\frac1n\sum_{i=1}^{n}(x_i-\mu)^2}
$$

---

## Complex Numbers

Euler's identity

$$
e^{i\pi}+1=0
$$

The magnitude of a complex number is

$$
|z|=\sqrt{a^2+b^2}
$$

---

## Trigonometry

The identity

$$
\sin^2x+\cos^2x=1
$$

The double-angle formula

$$
\sin(2x)=2\sin x\cos x
$$

---

## Logarithms

$$
\log(ab)=\log a+\log b
$$

$$
\ln(e^x)=x
$$

---

## Differential Equations

$$
\frac{dy}{dx}=ky
$$

whose solution is

$$
y=Ce^{kx}
$$

---

## Inline Examples

The area of a circle is $A=\pi r^2$.

The speed is $v=\frac{d}{t}$.

The energy equation is $E=mc^2$.

The roots satisfy $x_1+x_2=-\frac{b}{a}$.

The sequence is $a_n=2n+1$.

The limit $\lim_{n\to\infty}\frac1n=0$.

The probability is $\frac38$.

The vector is $\vec{v}=(1,2,3)$.

The angle is $90^\circ$.

The function grows like $O(n\log n)$.